# File này quản lý thông tin xác thực cho các kênh giao tiếp

# 1. Kết nối với React Frontend qua Socket.io (BẮT BUỘC)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# 2. Cho phép chạy trên terminal (mặc định của Rasa)
rest:
  url: "http://localhost:5005"
