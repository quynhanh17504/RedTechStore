# File này định nghĩa các điểm cuối (endpoints) mà Rasa sẽ kết nối tới.

# 1. Kết nối tới Action Server (Python)
# Rasa sẽ gửi yêu cầu tới đây khi gặp các action bắt đầu bằng 'action_'
action_endpoint:
  url: "http://localhost:5055/webhook"

# 2. Tracker Store (Tùy chọn)
# Nơi lưu trữ hội thoại. Mặc định Rasa lưu trong bộ nhớ (InMemory).
# Nếu muốn lưu vào SQL để theo dõi lịch sử, bạn có thể cấu hình ở đây.
# tracker_store:
#    type: sql
#    dialect: "mysql"
#    url: "localhost"
#    db: "redtech_chatbot_history"
#    username: "root"
#    password: ""

# 3. Event Broker (Tùy chọn)
# Nếu bạn muốn gửi dữ liệu hội thoại sang một dịch vụ khác (như RabbitMQ)
# event_broker:
#   type: "pika"
#   url: "localhost"